services:
  api:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app  # Mount the current directory to /app in the container
    environment:
      - PYTHONUNBUFFERED=1
      - FLASK_APP=app:app
      - FLASK_ENV=development
    restart: unless-stopped

